{
  "name": "qa-playwright-saucedemo-demo",
  "version": "1.2.0",
  "description": "QA portfolio project for SauceDemo covering manual functional testing, defect documentation, traceability, and Playwright automation with JavaScript.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "test:debug": "playwright test --debug",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "latest"
  }
}
